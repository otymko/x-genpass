метод Скрипт()
    знч ТекущийКаталог = новый Файл(Скрипт.ПутьНачальногоСкрипта).Каталог!.Каталог
    знч КаталогТестов = новый Файл("tests", ТекущийКаталог.Путь)

    пер НастройкиПоиска = новый НастройкиПоиска()
    НастройкиПоиска.ИмяСодержит(".sbsl")
    знч НайденныеФайлы = Файлы.Найти(КаталогТестов, НастройкиПоиска = НастройкиПоиска)
    для ФайлТестов из НайденныеФайлы
        Консоль.Записать("Прогон тестов из " + ФайлТестов.Имя)
        пер Процесс = новый ПроцессОс("executor", [ФайлТестов.Путь], Истина)
        Процесс.Запустить()
        Процесс.ОжидатьЗавершения()
        // TODO нужен вывод в текущий пайп консоли в момент выполнения процесса
        Консоль.Записать(Процесс.ПолучитьПотокВывода().ПрочитатьКакСтроку())
    ;
;